let a = 15;
let b = 20;

console.log('The value of a is => '+a+ ' ' +' The value of a is => '+b);

[a,b] = [b,a]

// after swapped value
console.log('After swappped the value of a now is '+a+"  and the value of b nor is "+b);
